def first_last_equal(numbers):
    """
    Check if the first and last number of a given list are the same.
    If they are the same, return True. Otherwise, return False.
    """
    if numbers[0] == numbers[-1]:
        return True
    else:
        return False
